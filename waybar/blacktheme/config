{
  "layer": "top",
  "width": 1920,
  "modules-left": ["hyprland/workspaces"],
  "modules-center": ["custom/media", "pulseaudio"],
  "modules-right": ["hyprland/langugae", "custom/updates", "cpu", "tray", "clock"],

  "hyprland/workspaces": {
    "disable-scroll": true,
    "persistent_workspaces": {
      "1": [],
      "2": [],
      "3": [],
      "4": [],
    },
  },

  "hyprland/language": {
    "format": "{}",
    "min-length": 5,
    "tooltip": false,
  },


  "custom/media": {
    "format": {},
    "tooltip": {},
    "return-type": "json",
    "max-length":40, 
    "interval": 1,
    "exec": "$HOME/.config/waybar/scripts/mpw --music",
  },

  "pulseaudio": {
    "tooltip": false,
    "scroll-step": 5,
    "format": "{icon} {volume}%",
    "on-click": "pavucontrol",
    "format-icons": {
      "default": ["", "", ""]
    },
  },

  "cpu": {
    "interval": 5,
    "format": "{usage}%",
    "tooltip": false
  },

  "tray": {
    "icon-size": 20,
    "spacing": 10,
    "tooltip": false,
  },

  "clock": {
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>",
        "interval": 60,
        "format": "{:%H:%M}",
        "max-length": 25
  },

  "custom/updates": {
    "format": "{} 󰣇",
    "exec": "checkupdates | wc -l",
    "exec-if": "[[ $(checkupdates | wc -l) != 0 ]]",
    // "interval": 60,
    "on-click": "alacritty -e sudo pacman -Syu && notify-send 'Система обновленна! Спасибо 󰱳' && pkill -SIGRTMIN+8 waybar",
    "signal": 8, // Периписать на раст
  },

  "custom/mem": {
    "format": "{} 󰕪",
    "interval": 3,
    "exec": "free -h | awk '/Mem:/{printf $3}'",
    "tooltip": false,
  },

}
